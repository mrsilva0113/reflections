What happens when you initialize a repository? Why do you need to do it?

Git sets up an environment to track changes made to files.

How is the staging area different from the working directory and the repository? What value do you think it offers?

Staging area allows you setup which files you want track versions.

How can you use the staging area to make sure you have one commit per logical change?

I am able to add files to staging area with changes I am ready to commit.  Before committing the files I am able to compare the file or files to the master branch.

What are some situations when branches would be helpful in keeping your history organized? How would branches help?

Branches allows there to be several working versions of code.  With multiple versions you can try different ideas to write the code.  

How do the diagrams help you visualize the branch structure?

Diagram let me see how the branches and commits work together and to is if any commits are unreachable.

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?

The result is two branches become one and from the one you can see the commits made on each branch.

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?

Automatic merges make the code merge easier.  Manual merges allow the developers merge conflicts in code.